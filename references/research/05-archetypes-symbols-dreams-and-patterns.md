# 原型、象征、梦与模式 / Archetypes, Symbols, Dreams, and Patterns

Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification.

## Why This File Exists

- This file supports the Carl Jung person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 荣格 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### archetypes — Definition

- What this names: within `Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification`, `archetypes` points to a concrete node rather than a prestige label.
- Object boundary: treat `archetypes` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `archetypes` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `archetypes` before pulling it into a modern analogy.
- Skill value: `archetypes` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `archetypes` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `archetypes` clearly enough that a later answer can say what `archetypes` includes and excludes.

### archetypes — Historical Context

- Historical location: ask when `archetypes` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `archetypes` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `archetypes` says, but why `archetypes` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `archetypes` from later packaging or selective reuse.
- Reading move: map `archetypes` against the timeline before treating `archetypes` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `archetypes` before extracting any lesson from `archetypes`.
- Failure risk: without context, `archetypes` can look cleaner, more universal, and more innocent than it actually was.

### archetypes — Internal Tension

- Main tension: `archetypes` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `archetypes` makes possible and what `archetypes` quietly suppresses at the same time.
- Interpretive value: the tension in `archetypes` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `archetypes` double-sided rather than converting `archetypes` into moral reassurance.
- Research warning: if every sentence about `archetypes` sounds admiring or dismissive, `archetypes` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `archetypes`.
- Better question: what does `archetypes` solve, and what new silence does `archetypes` create?

### archetypes — Key Distinction

- Core distinction: separate `archetypes` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `archetypes`.
- Typical confusion: readers often mistake `archetypes` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `archetypes` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `archetypes`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `archetypes` by accident.
- Operational cue: define what `archetypes` is not before claiming to know what `archetypes` is.

### archetypes — Method

- Method question: how does `archetypes` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `archetypes` rather than inspirational phrasing.
- Research use: `archetypes` matters when `archetypes` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `archetypes`, not costume around `archetypes`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `archetypes`.
- Modern move: only transfer the mechanism in `archetypes` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `archetypes` without reproducing the method inside `archetypes`.

### archetypes — Operational Use

- Operational role: `archetypes` belongs in the skill because `archetypes` changes how prompts are framed and how problems are cut.
- Decision value: `archetypes` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `archetypes` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `archetypes` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `archetypes` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `archetypes` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `archetypes` make me check before I act?

### archetypes — Transfer To Modern Problems

- Transfer principle: bring `archetypes` forward only when the structural analogy around `archetypes` is stronger than the historical costume.
- Modern fit: `archetypes` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `archetypes` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `archetypes`, not the surrounding mythology around `archetypes`.
- Bad transfer: using `archetypes` to justify positions `archetypes` was never built to adjudicate.
- Practical test: if the analogy for `archetypes` cannot survive plainer language, `archetypes` probably does not belong in the answer.
- Design implication: every modern use of `archetypes` should make the analogy explicit instead of pretending `archetypes` is self-evident.

### archetypes — Failure Mode

- Failure pattern: `archetypes` goes bad when `archetypes` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `archetypes` for authority, mood, or aesthetic and lose the underlying discipline in `archetypes`.
- Research signal: when `archetypes` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `archetypes`.
- User risk: answers can start sounding impressive while `archetypes` becomes less falsifiable.
- Repository risk: `archetypes` can silently drift from operating concept into mascot.
- Correction move: pull `archetypes` back to evidence, mechanism, and concrete limits.
- Best warning label: `archetypes` should increase rigor, not lower the burden of proof.

### archetypes — Common Misreading

- Typical misreading: `archetypes` is often taken as a broad worldview when `archetypes` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `archetypes`.
- Consequence: readers then import claims into `archetypes` that the corpus does not actually support for `archetypes`.
- Skill response: keep `archetypes` attached to the texts, cases, and institutional situations that make `archetypes` legible.
- Modern correction: show what `archetypes` can illuminate without pretending `archetypes` answers everything nearby.
- Language fix: name the overreach around `archetypes` directly instead of just gesturing at nuance.
- Reader test: if `archetypes` suddenly explains every problem, `archetypes` is being misread.

### archetypes — Boundary Condition

- Boundary condition: `archetypes` only travels well when the receiving problem shares enough structure with the original one behind `archetypes`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `archetypes`.
- Ethical test: some uses of `archetypes` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `archetypes` into careless advice.
- Good limit: it is fine for `archetypes` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `archetypes` has no boundary because `archetypes` feels generically wise.
- Operational question: under what conditions should `archetypes` be left unused?

### archetypes — Better Question

- Better question: instead of asking whether `archetypes` is simply true, ask what problem `archetypes` was trying to solve.
- Reader upgrade: this turns `archetypes` from a label into a live diagnostic instrument.
- Research upgrade: `archetypes` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `archetypes` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `archetypes` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `archetypes` becomes useful when `archetypes` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `archetypes` is framed as a problem-solving move?

### archetypes — Example Move

- Example move: use `archetypes` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `archetypes` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `archetypes` is strongest when `archetypes` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `archetypes` lets the user do that they could not do before.
- Transfer discipline: keep the move around `archetypes` small and explicit so the analogy does not sprawl.
- Repository standard: if `archetypes` never produces a concrete move, `archetypes` is not yet operational.
- User question: what is one thing `archetypes` would make me inspect differently right now?

### archetypes — What To Preserve

- Preserve the pressure: `archetypes` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `archetypes` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `archetypes`.
- Preserve the routing value: `archetypes` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `archetypes` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `archetypes` stops is part of preserving `archetypes` well.
- Preserve the usefulness: a transferred `archetypes` should still help someone make a cleaner judgment.

### archetypes — What To Drop

- Drop the costume: `archetypes` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `archetypes` has been understood.
- Drop the universal claim: `archetypes` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `archetypes` survives only as a catchphrase, `archetypes` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `archetypes` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `archetypes` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `archetypes` and remove the surrounding noise.

### archetypes — Design Implication

- Design implication: `archetypes` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `archetypes` that later edits can stay honest.
- UX implication: users should feel `archetypes` changing the angle of analysis, not just the tone.
- Evaluation implication: test `archetypes` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `archetypes` only if they sharpen the boundary of `archetypes` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `archetypes` should refuse.
- Bottom line: `archetypes` belongs in the repo only if `archetypes` keeps improving judgment after the historical costume is removed.

### symbolic language — Definition

- What this names: within `Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification`, `symbolic language` points to a concrete node rather than a prestige label.
- Object boundary: treat `symbolic language` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `symbolic language` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `symbolic language` before pulling it into a modern analogy.
- Skill value: `symbolic language` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `symbolic language` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `symbolic language` clearly enough that a later answer can say what `symbolic language` includes and excludes.

### symbolic language — Historical Context

- Historical location: ask when `symbolic language` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `symbolic language` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `symbolic language` says, but why `symbolic language` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `symbolic language` from later packaging or selective reuse.
- Reading move: map `symbolic language` against the timeline before treating `symbolic language` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `symbolic language` before extracting any lesson from `symbolic language`.
- Failure risk: without context, `symbolic language` can look cleaner, more universal, and more innocent than it actually was.

### symbolic language — Internal Tension

- Main tension: `symbolic language` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `symbolic language` makes possible and what `symbolic language` quietly suppresses at the same time.
- Interpretive value: the tension in `symbolic language` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `symbolic language` double-sided rather than converting `symbolic language` into moral reassurance.
- Research warning: if every sentence about `symbolic language` sounds admiring or dismissive, `symbolic language` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `symbolic language`.
- Better question: what does `symbolic language` solve, and what new silence does `symbolic language` create?

### symbolic language — Key Distinction

- Core distinction: separate `symbolic language` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `symbolic language`.
- Typical confusion: readers often mistake `symbolic language` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `symbolic language` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `symbolic language`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `symbolic language` by accident.
- Operational cue: define what `symbolic language` is not before claiming to know what `symbolic language` is.

### symbolic language — Method

- Method question: how does `symbolic language` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `symbolic language` rather than inspirational phrasing.
- Research use: `symbolic language` matters when `symbolic language` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `symbolic language`, not costume around `symbolic language`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `symbolic language`.
- Modern move: only transfer the mechanism in `symbolic language` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `symbolic language` without reproducing the method inside `symbolic language`.

### symbolic language — Operational Use

- Operational role: `symbolic language` belongs in the skill because `symbolic language` changes how prompts are framed and how problems are cut.
- Decision value: `symbolic language` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `symbolic language` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `symbolic language` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `symbolic language` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `symbolic language` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `symbolic language` make me check before I act?

### symbolic language — Transfer To Modern Problems

- Transfer principle: bring `symbolic language` forward only when the structural analogy around `symbolic language` is stronger than the historical costume.
- Modern fit: `symbolic language` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `symbolic language` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `symbolic language`, not the surrounding mythology around `symbolic language`.
- Bad transfer: using `symbolic language` to justify positions `symbolic language` was never built to adjudicate.
- Practical test: if the analogy for `symbolic language` cannot survive plainer language, `symbolic language` probably does not belong in the answer.
- Design implication: every modern use of `symbolic language` should make the analogy explicit instead of pretending `symbolic language` is self-evident.

### symbolic language — Failure Mode

- Failure pattern: `symbolic language` goes bad when `symbolic language` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `symbolic language` for authority, mood, or aesthetic and lose the underlying discipline in `symbolic language`.
- Research signal: when `symbolic language` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `symbolic language`.
- User risk: answers can start sounding impressive while `symbolic language` becomes less falsifiable.
- Repository risk: `symbolic language` can silently drift from operating concept into mascot.
- Correction move: pull `symbolic language` back to evidence, mechanism, and concrete limits.
- Best warning label: `symbolic language` should increase rigor, not lower the burden of proof.

### symbolic language — Common Misreading

- Typical misreading: `symbolic language` is often taken as a broad worldview when `symbolic language` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `symbolic language`.
- Consequence: readers then import claims into `symbolic language` that the corpus does not actually support for `symbolic language`.
- Skill response: keep `symbolic language` attached to the texts, cases, and institutional situations that make `symbolic language` legible.
- Modern correction: show what `symbolic language` can illuminate without pretending `symbolic language` answers everything nearby.
- Language fix: name the overreach around `symbolic language` directly instead of just gesturing at nuance.
- Reader test: if `symbolic language` suddenly explains every problem, `symbolic language` is being misread.

### symbolic language — Boundary Condition

- Boundary condition: `symbolic language` only travels well when the receiving problem shares enough structure with the original one behind `symbolic language`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `symbolic language`.
- Ethical test: some uses of `symbolic language` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `symbolic language` into careless advice.
- Good limit: it is fine for `symbolic language` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `symbolic language` has no boundary because `symbolic language` feels generically wise.
- Operational question: under what conditions should `symbolic language` be left unused?

### symbolic language — Better Question

- Better question: instead of asking whether `symbolic language` is simply true, ask what problem `symbolic language` was trying to solve.
- Reader upgrade: this turns `symbolic language` from a label into a live diagnostic instrument.
- Research upgrade: `symbolic language` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `symbolic language` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `symbolic language` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `symbolic language` becomes useful when `symbolic language` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `symbolic language` is framed as a problem-solving move?

### symbolic language — Example Move

- Example move: use `symbolic language` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `symbolic language` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `symbolic language` is strongest when `symbolic language` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `symbolic language` lets the user do that they could not do before.
- Transfer discipline: keep the move around `symbolic language` small and explicit so the analogy does not sprawl.
- Repository standard: if `symbolic language` never produces a concrete move, `symbolic language` is not yet operational.
- User question: what is one thing `symbolic language` would make me inspect differently right now?

### symbolic language — What To Preserve

- Preserve the pressure: `symbolic language` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `symbolic language` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `symbolic language`.
- Preserve the routing value: `symbolic language` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `symbolic language` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `symbolic language` stops is part of preserving `symbolic language` well.
- Preserve the usefulness: a transferred `symbolic language` should still help someone make a cleaner judgment.

### symbolic language — What To Drop

- Drop the costume: `symbolic language` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `symbolic language` has been understood.
- Drop the universal claim: `symbolic language` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `symbolic language` survives only as a catchphrase, `symbolic language` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `symbolic language` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `symbolic language` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `symbolic language` and remove the surrounding noise.

### symbolic language — Design Implication

- Design implication: `symbolic language` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `symbolic language` that later edits can stay honest.
- UX implication: users should feel `symbolic language` changing the angle of analysis, not just the tone.
- Evaluation implication: test `symbolic language` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `symbolic language` only if they sharpen the boundary of `symbolic language` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `symbolic language` should refuse.
- Bottom line: `symbolic language` belongs in the repo only if `symbolic language` keeps improving judgment after the historical costume is removed.

### dream material — Definition

- What this names: within `Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification`, `dream material` points to a concrete node rather than a prestige label.
- Object boundary: treat `dream material` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `dream material` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `dream material` before pulling it into a modern analogy.
- Skill value: `dream material` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `dream material` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `dream material` clearly enough that a later answer can say what `dream material` includes and excludes.

### dream material — Historical Context

- Historical location: ask when `dream material` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `dream material` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `dream material` says, but why `dream material` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `dream material` from later packaging or selective reuse.
- Reading move: map `dream material` against the timeline before treating `dream material` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `dream material` before extracting any lesson from `dream material`.
- Failure risk: without context, `dream material` can look cleaner, more universal, and more innocent than it actually was.

### dream material — Internal Tension

- Main tension: `dream material` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `dream material` makes possible and what `dream material` quietly suppresses at the same time.
- Interpretive value: the tension in `dream material` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `dream material` double-sided rather than converting `dream material` into moral reassurance.
- Research warning: if every sentence about `dream material` sounds admiring or dismissive, `dream material` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `dream material`.
- Better question: what does `dream material` solve, and what new silence does `dream material` create?

### dream material — Key Distinction

- Core distinction: separate `dream material` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `dream material`.
- Typical confusion: readers often mistake `dream material` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `dream material` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `dream material`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `dream material` by accident.
- Operational cue: define what `dream material` is not before claiming to know what `dream material` is.

### dream material — Method

- Method question: how does `dream material` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `dream material` rather than inspirational phrasing.
- Research use: `dream material` matters when `dream material` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `dream material`, not costume around `dream material`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `dream material`.
- Modern move: only transfer the mechanism in `dream material` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `dream material` without reproducing the method inside `dream material`.

### dream material — Operational Use

- Operational role: `dream material` belongs in the skill because `dream material` changes how prompts are framed and how problems are cut.
- Decision value: `dream material` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `dream material` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `dream material` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `dream material` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `dream material` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `dream material` make me check before I act?

### dream material — Transfer To Modern Problems

- Transfer principle: bring `dream material` forward only when the structural analogy around `dream material` is stronger than the historical costume.
- Modern fit: `dream material` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `dream material` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `dream material`, not the surrounding mythology around `dream material`.
- Bad transfer: using `dream material` to justify positions `dream material` was never built to adjudicate.
- Practical test: if the analogy for `dream material` cannot survive plainer language, `dream material` probably does not belong in the answer.
- Design implication: every modern use of `dream material` should make the analogy explicit instead of pretending `dream material` is self-evident.

### dream material — Failure Mode

- Failure pattern: `dream material` goes bad when `dream material` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `dream material` for authority, mood, or aesthetic and lose the underlying discipline in `dream material`.
- Research signal: when `dream material` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `dream material`.
- User risk: answers can start sounding impressive while `dream material` becomes less falsifiable.
- Repository risk: `dream material` can silently drift from operating concept into mascot.
- Correction move: pull `dream material` back to evidence, mechanism, and concrete limits.
- Best warning label: `dream material` should increase rigor, not lower the burden of proof.

### dream material — Common Misreading

- Typical misreading: `dream material` is often taken as a broad worldview when `dream material` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `dream material`.
- Consequence: readers then import claims into `dream material` that the corpus does not actually support for `dream material`.
- Skill response: keep `dream material` attached to the texts, cases, and institutional situations that make `dream material` legible.
- Modern correction: show what `dream material` can illuminate without pretending `dream material` answers everything nearby.
- Language fix: name the overreach around `dream material` directly instead of just gesturing at nuance.
- Reader test: if `dream material` suddenly explains every problem, `dream material` is being misread.

### dream material — Boundary Condition

- Boundary condition: `dream material` only travels well when the receiving problem shares enough structure with the original one behind `dream material`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `dream material`.
- Ethical test: some uses of `dream material` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `dream material` into careless advice.
- Good limit: it is fine for `dream material` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `dream material` has no boundary because `dream material` feels generically wise.
- Operational question: under what conditions should `dream material` be left unused?

### dream material — Better Question

- Better question: instead of asking whether `dream material` is simply true, ask what problem `dream material` was trying to solve.
- Reader upgrade: this turns `dream material` from a label into a live diagnostic instrument.
- Research upgrade: `dream material` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `dream material` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `dream material` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `dream material` becomes useful when `dream material` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `dream material` is framed as a problem-solving move?

### dream material — Example Move

- Example move: use `dream material` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `dream material` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `dream material` is strongest when `dream material` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `dream material` lets the user do that they could not do before.
- Transfer discipline: keep the move around `dream material` small and explicit so the analogy does not sprawl.
- Repository standard: if `dream material` never produces a concrete move, `dream material` is not yet operational.
- User question: what is one thing `dream material` would make me inspect differently right now?

### dream material — What To Preserve

- Preserve the pressure: `dream material` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `dream material` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `dream material`.
- Preserve the routing value: `dream material` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `dream material` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `dream material` stops is part of preserving `dream material` well.
- Preserve the usefulness: a transferred `dream material` should still help someone make a cleaner judgment.

### dream material — What To Drop

- Drop the costume: `dream material` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `dream material` has been understood.
- Drop the universal claim: `dream material` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `dream material` survives only as a catchphrase, `dream material` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `dream material` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `dream material` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `dream material` and remove the surrounding noise.

### dream material — Design Implication

- Design implication: `dream material` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `dream material` that later edits can stay honest.
- UX implication: users should feel `dream material` changing the angle of analysis, not just the tone.
- Evaluation implication: test `dream material` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `dream material` only if they sharpen the boundary of `dream material` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `dream material` should refuse.
- Bottom line: `dream material` belongs in the repo only if `dream material` keeps improving judgment after the historical costume is removed.

### narrative and pattern — Definition

- What this names: within `Archetypes, symbols, dreams, and how pattern language can help without collapsing into mystification`, `narrative and pattern` points to a concrete node rather than a prestige label.
- Object boundary: treat `narrative and pattern` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `narrative and pattern` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `narrative and pattern` before pulling it into a modern analogy.
- Skill value: `narrative and pattern` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `narrative and pattern` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `narrative and pattern` clearly enough that a later answer can say what `narrative and pattern` includes and excludes.

### narrative and pattern — Historical Context

- Historical location: ask when `narrative and pattern` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `narrative and pattern` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `narrative and pattern` says, but why `narrative and pattern` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `narrative and pattern` from later packaging or selective reuse.
- Reading move: map `narrative and pattern` against the timeline before treating `narrative and pattern` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `narrative and pattern` before extracting any lesson from `narrative and pattern`.
- Failure risk: without context, `narrative and pattern` can look cleaner, more universal, and more innocent than it actually was.

### narrative and pattern — Internal Tension

- Main tension: `narrative and pattern` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `narrative and pattern` makes possible and what `narrative and pattern` quietly suppresses at the same time.
- Interpretive value: the tension in `narrative and pattern` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `narrative and pattern` double-sided rather than converting `narrative and pattern` into moral reassurance.
- Research warning: if every sentence about `narrative and pattern` sounds admiring or dismissive, `narrative and pattern` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `narrative and pattern`.
- Better question: what does `narrative and pattern` solve, and what new silence does `narrative and pattern` create?

### narrative and pattern — Key Distinction

- Core distinction: separate `narrative and pattern` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `narrative and pattern`.
- Typical confusion: readers often mistake `narrative and pattern` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `narrative and pattern` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `narrative and pattern`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `narrative and pattern` by accident.
- Operational cue: define what `narrative and pattern` is not before claiming to know what `narrative and pattern` is.

### narrative and pattern — Method

- Method question: how does `narrative and pattern` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `narrative and pattern` rather than inspirational phrasing.
- Research use: `narrative and pattern` matters when `narrative and pattern` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `narrative and pattern`, not costume around `narrative and pattern`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `narrative and pattern`.
- Modern move: only transfer the mechanism in `narrative and pattern` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `narrative and pattern` without reproducing the method inside `narrative and pattern`.

### narrative and pattern — Operational Use

- Operational role: `narrative and pattern` belongs in the skill because `narrative and pattern` changes how prompts are framed and how problems are cut.
- Decision value: `narrative and pattern` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `narrative and pattern` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `narrative and pattern` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `narrative and pattern` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `narrative and pattern` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `narrative and pattern` make me check before I act?

### narrative and pattern — Transfer To Modern Problems

- Transfer principle: bring `narrative and pattern` forward only when the structural analogy around `narrative and pattern` is stronger than the historical costume.
- Modern fit: `narrative and pattern` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `narrative and pattern` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `narrative and pattern`, not the surrounding mythology around `narrative and pattern`.
- Bad transfer: using `narrative and pattern` to justify positions `narrative and pattern` was never built to adjudicate.
- Practical test: if the analogy for `narrative and pattern` cannot survive plainer language, `narrative and pattern` probably does not belong in the answer.
- Design implication: every modern use of `narrative and pattern` should make the analogy explicit instead of pretending `narrative and pattern` is self-evident.

### narrative and pattern — Failure Mode

- Failure pattern: `narrative and pattern` goes bad when `narrative and pattern` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `narrative and pattern` for authority, mood, or aesthetic and lose the underlying discipline in `narrative and pattern`.
- Research signal: when `narrative and pattern` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `narrative and pattern`.
- User risk: answers can start sounding impressive while `narrative and pattern` becomes less falsifiable.
- Repository risk: `narrative and pattern` can silently drift from operating concept into mascot.
- Correction move: pull `narrative and pattern` back to evidence, mechanism, and concrete limits.
- Best warning label: `narrative and pattern` should increase rigor, not lower the burden of proof.

### narrative and pattern — Common Misreading

- Typical misreading: `narrative and pattern` is often taken as a broad worldview when `narrative and pattern` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `narrative and pattern`.
- Consequence: readers then import claims into `narrative and pattern` that the corpus does not actually support for `narrative and pattern`.
- Skill response: keep `narrative and pattern` attached to the texts, cases, and institutional situations that make `narrative and pattern` legible.
- Modern correction: show what `narrative and pattern` can illuminate without pretending `narrative and pattern` answers everything nearby.
- Language fix: name the overreach around `narrative and pattern` directly instead of just gesturing at nuance.
- Reader test: if `narrative and pattern` suddenly explains every problem, `narrative and pattern` is being misread.

### narrative and pattern — Boundary Condition

- Boundary condition: `narrative and pattern` only travels well when the receiving problem shares enough structure with the original one behind `narrative and pattern`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `narrative and pattern`.
- Ethical test: some uses of `narrative and pattern` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `narrative and pattern` into careless advice.
- Good limit: it is fine for `narrative and pattern` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `narrative and pattern` has no boundary because `narrative and pattern` feels generically wise.
- Operational question: under what conditions should `narrative and pattern` be left unused?

### narrative and pattern — Better Question

- Better question: instead of asking whether `narrative and pattern` is simply true, ask what problem `narrative and pattern` was trying to solve.
- Reader upgrade: this turns `narrative and pattern` from a label into a live diagnostic instrument.
- Research upgrade: `narrative and pattern` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `narrative and pattern` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `narrative and pattern` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `narrative and pattern` becomes useful when `narrative and pattern` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `narrative and pattern` is framed as a problem-solving move?

### narrative and pattern — Example Move

- Example move: use `narrative and pattern` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `narrative and pattern` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `narrative and pattern` is strongest when `narrative and pattern` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `narrative and pattern` lets the user do that they could not do before.
- Transfer discipline: keep the move around `narrative and pattern` small and explicit so the analogy does not sprawl.
- Repository standard: if `narrative and pattern` never produces a concrete move, `narrative and pattern` is not yet operational.
- User question: what is one thing `narrative and pattern` would make me inspect differently right now?

### narrative and pattern — What To Preserve

- Preserve the pressure: `narrative and pattern` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `narrative and pattern` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `narrative and pattern`.
- Preserve the routing value: `narrative and pattern` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `narrative and pattern` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `narrative and pattern` stops is part of preserving `narrative and pattern` well.
- Preserve the usefulness: a transferred `narrative and pattern` should still help someone make a cleaner judgment.

### narrative and pattern — What To Drop

- Drop the costume: `narrative and pattern` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `narrative and pattern` has been understood.
- Drop the universal claim: `narrative and pattern` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `narrative and pattern` survives only as a catchphrase, `narrative and pattern` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `narrative and pattern` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `narrative and pattern` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `narrative and pattern` and remove the surrounding noise.

### narrative and pattern — Design Implication

- Design implication: `narrative and pattern` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `narrative and pattern` that later edits can stay honest.
- UX implication: users should feel `narrative and pattern` changing the angle of analysis, not just the tone.
- Evaluation implication: test `narrative and pattern` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `narrative and pattern` only if they sharpen the boundary of `narrative and pattern` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `narrative and pattern` should refuse.
- Bottom line: `narrative and pattern` belongs in the repo only if `narrative and pattern` keeps improving judgment after the historical costume is removed.


## Source Files

这些调研文件回链到本仓库 `references/sources/` 里的原始素材层。

- [`archetypes-and-collective-unconscious-source-card.md`](../sources/books/archetypes-and-collective-unconscious-source-card.md)
- [`psychological-types.md`](../sources/books/psychological-types.md)
- [`two-essays-on-analytical-psychology-source-card.md`](../sources/books/two-essays-on-analytical-psychology-source-card.md)
- [`britannica-jung.md`](../sources/articles/britannica-jung.md)
- [`iep-jung.md`](../sources/articles/iep-jung.md)

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[archetypes-and-collective-unconscious-source-card.md](../sources/books/archetypes-and-collective-unconscious-source-card.md)、[psychological-types.md](../sources/books/psychological-types.md)、[two-essays-on-analytical-psychology-source-card.md](../sources/books/two-essays-on-analytical-psychology-source-card.md)
- transcripts：（无）
- articles：[britannica-jung.md](../sources/articles/britannica-jung.md)、[iep-jung.md](../sources/articles/iep-jung.md)
