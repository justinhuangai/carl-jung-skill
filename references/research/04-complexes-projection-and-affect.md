# 情结、投射与情感强度 / Complexes, Projection, and Affect

Complexes, projection, charged reactions, and how inner structures shape outer conflict.

## Why This File Exists

- This file supports the Carl Jung person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 荣格 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is Complexes, projection, charged reactions, and how inner structures shape outer conflict..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### complexes — Definition

- What this names: within `Complexes, projection, charged reactions, and how inner structures shape outer conflict`, `complexes` points to a concrete node rather than a prestige label.
- Object boundary: treat `complexes` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `complexes` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `complexes` before pulling it into a modern analogy.
- Skill value: `complexes` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `complexes` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `complexes` clearly enough that a later answer can say what `complexes` includes and excludes.

### complexes — Historical Context

- Historical location: ask when `complexes` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `complexes` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `complexes` says, but why `complexes` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `complexes` from later packaging or selective reuse.
- Reading move: map `complexes` against the timeline before treating `complexes` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `complexes` before extracting any lesson from `complexes`.
- Failure risk: without context, `complexes` can look cleaner, more universal, and more innocent than it actually was.

### complexes — Internal Tension

- Main tension: `complexes` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `complexes` makes possible and what `complexes` quietly suppresses at the same time.
- Interpretive value: the tension in `complexes` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `complexes` double-sided rather than converting `complexes` into moral reassurance.
- Research warning: if every sentence about `complexes` sounds admiring or dismissive, `complexes` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `complexes`.
- Better question: what does `complexes` solve, and what new silence does `complexes` create?

### complexes — Key Distinction

- Core distinction: separate `complexes` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `complexes`.
- Typical confusion: readers often mistake `complexes` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `complexes` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `complexes`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `complexes` by accident.
- Operational cue: define what `complexes` is not before claiming to know what `complexes` is.

### complexes — Method

- Method question: how does `complexes` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `complexes` rather than inspirational phrasing.
- Research use: `complexes` matters when `complexes` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `complexes`, not costume around `complexes`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `complexes`.
- Modern move: only transfer the mechanism in `complexes` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `complexes` without reproducing the method inside `complexes`.

### complexes — Operational Use

- Operational role: `complexes` belongs in the skill because `complexes` changes how prompts are framed and how problems are cut.
- Decision value: `complexes` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `complexes` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `complexes` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `complexes` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `complexes` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `complexes` make me check before I act?

### complexes — Transfer To Modern Problems

- Transfer principle: bring `complexes` forward only when the structural analogy around `complexes` is stronger than the historical costume.
- Modern fit: `complexes` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `complexes` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `complexes`, not the surrounding mythology around `complexes`.
- Bad transfer: using `complexes` to justify positions `complexes` was never built to adjudicate.
- Practical test: if the analogy for `complexes` cannot survive plainer language, `complexes` probably does not belong in the answer.
- Design implication: every modern use of `complexes` should make the analogy explicit instead of pretending `complexes` is self-evident.

### complexes — Failure Mode

- Failure pattern: `complexes` goes bad when `complexes` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `complexes` for authority, mood, or aesthetic and lose the underlying discipline in `complexes`.
- Research signal: when `complexes` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `complexes`.
- User risk: answers can start sounding impressive while `complexes` becomes less falsifiable.
- Repository risk: `complexes` can silently drift from operating concept into mascot.
- Correction move: pull `complexes` back to evidence, mechanism, and concrete limits.
- Best warning label: `complexes` should increase rigor, not lower the burden of proof.

### complexes — Common Misreading

- Typical misreading: `complexes` is often taken as a broad worldview when `complexes` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `complexes`.
- Consequence: readers then import claims into `complexes` that the corpus does not actually support for `complexes`.
- Skill response: keep `complexes` attached to the texts, cases, and institutional situations that make `complexes` legible.
- Modern correction: show what `complexes` can illuminate without pretending `complexes` answers everything nearby.
- Language fix: name the overreach around `complexes` directly instead of just gesturing at nuance.
- Reader test: if `complexes` suddenly explains every problem, `complexes` is being misread.

### complexes — Boundary Condition

- Boundary condition: `complexes` only travels well when the receiving problem shares enough structure with the original one behind `complexes`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `complexes`.
- Ethical test: some uses of `complexes` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `complexes` into careless advice.
- Good limit: it is fine for `complexes` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `complexes` has no boundary because `complexes` feels generically wise.
- Operational question: under what conditions should `complexes` be left unused?

### complexes — Better Question

- Better question: instead of asking whether `complexes` is simply true, ask what problem `complexes` was trying to solve.
- Reader upgrade: this turns `complexes` from a label into a live diagnostic instrument.
- Research upgrade: `complexes` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `complexes` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `complexes` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `complexes` becomes useful when `complexes` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `complexes` is framed as a problem-solving move?

### complexes — Example Move

- Example move: use `complexes` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `complexes` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `complexes` is strongest when `complexes` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `complexes` lets the user do that they could not do before.
- Transfer discipline: keep the move around `complexes` small and explicit so the analogy does not sprawl.
- Repository standard: if `complexes` never produces a concrete move, `complexes` is not yet operational.
- User question: what is one thing `complexes` would make me inspect differently right now?

### complexes — What To Preserve

- Preserve the pressure: `complexes` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `complexes` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `complexes`.
- Preserve the routing value: `complexes` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `complexes` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `complexes` stops is part of preserving `complexes` well.
- Preserve the usefulness: a transferred `complexes` should still help someone make a cleaner judgment.

### complexes — What To Drop

- Drop the costume: `complexes` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `complexes` has been understood.
- Drop the universal claim: `complexes` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `complexes` survives only as a catchphrase, `complexes` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `complexes` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `complexes` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `complexes` and remove the surrounding noise.

### complexes — Design Implication

- Design implication: `complexes` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `complexes` that later edits can stay honest.
- UX implication: users should feel `complexes` changing the angle of analysis, not just the tone.
- Evaluation implication: test `complexes` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `complexes` only if they sharpen the boundary of `complexes` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `complexes` should refuse.
- Bottom line: `complexes` belongs in the repo only if `complexes` keeps improving judgment after the historical costume is removed.

### projection — Definition

- What this names: within `Complexes, projection, charged reactions, and how inner structures shape outer conflict`, `projection` points to a concrete node rather than a prestige label.
- Object boundary: treat `projection` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `projection` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `projection` before pulling it into a modern analogy.
- Skill value: `projection` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `projection` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `projection` clearly enough that a later answer can say what `projection` includes and excludes.

### projection — Historical Context

- Historical location: ask when `projection` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `projection` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `projection` says, but why `projection` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `projection` from later packaging or selective reuse.
- Reading move: map `projection` against the timeline before treating `projection` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `projection` before extracting any lesson from `projection`.
- Failure risk: without context, `projection` can look cleaner, more universal, and more innocent than it actually was.

### projection — Internal Tension

- Main tension: `projection` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `projection` makes possible and what `projection` quietly suppresses at the same time.
- Interpretive value: the tension in `projection` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `projection` double-sided rather than converting `projection` into moral reassurance.
- Research warning: if every sentence about `projection` sounds admiring or dismissive, `projection` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `projection`.
- Better question: what does `projection` solve, and what new silence does `projection` create?

### projection — Key Distinction

- Core distinction: separate `projection` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `projection`.
- Typical confusion: readers often mistake `projection` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `projection` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `projection`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `projection` by accident.
- Operational cue: define what `projection` is not before claiming to know what `projection` is.

### projection — Method

- Method question: how does `projection` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `projection` rather than inspirational phrasing.
- Research use: `projection` matters when `projection` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `projection`, not costume around `projection`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `projection`.
- Modern move: only transfer the mechanism in `projection` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `projection` without reproducing the method inside `projection`.

### projection — Operational Use

- Operational role: `projection` belongs in the skill because `projection` changes how prompts are framed and how problems are cut.
- Decision value: `projection` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `projection` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `projection` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `projection` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `projection` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `projection` make me check before I act?

### projection — Transfer To Modern Problems

- Transfer principle: bring `projection` forward only when the structural analogy around `projection` is stronger than the historical costume.
- Modern fit: `projection` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `projection` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `projection`, not the surrounding mythology around `projection`.
- Bad transfer: using `projection` to justify positions `projection` was never built to adjudicate.
- Practical test: if the analogy for `projection` cannot survive plainer language, `projection` probably does not belong in the answer.
- Design implication: every modern use of `projection` should make the analogy explicit instead of pretending `projection` is self-evident.

### projection — Failure Mode

- Failure pattern: `projection` goes bad when `projection` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `projection` for authority, mood, or aesthetic and lose the underlying discipline in `projection`.
- Research signal: when `projection` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `projection`.
- User risk: answers can start sounding impressive while `projection` becomes less falsifiable.
- Repository risk: `projection` can silently drift from operating concept into mascot.
- Correction move: pull `projection` back to evidence, mechanism, and concrete limits.
- Best warning label: `projection` should increase rigor, not lower the burden of proof.

### projection — Common Misreading

- Typical misreading: `projection` is often taken as a broad worldview when `projection` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `projection`.
- Consequence: readers then import claims into `projection` that the corpus does not actually support for `projection`.
- Skill response: keep `projection` attached to the texts, cases, and institutional situations that make `projection` legible.
- Modern correction: show what `projection` can illuminate without pretending `projection` answers everything nearby.
- Language fix: name the overreach around `projection` directly instead of just gesturing at nuance.
- Reader test: if `projection` suddenly explains every problem, `projection` is being misread.

### projection — Boundary Condition

- Boundary condition: `projection` only travels well when the receiving problem shares enough structure with the original one behind `projection`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `projection`.
- Ethical test: some uses of `projection` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `projection` into careless advice.
- Good limit: it is fine for `projection` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `projection` has no boundary because `projection` feels generically wise.
- Operational question: under what conditions should `projection` be left unused?

### projection — Better Question

- Better question: instead of asking whether `projection` is simply true, ask what problem `projection` was trying to solve.
- Reader upgrade: this turns `projection` from a label into a live diagnostic instrument.
- Research upgrade: `projection` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `projection` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `projection` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `projection` becomes useful when `projection` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `projection` is framed as a problem-solving move?

### projection — Example Move

- Example move: use `projection` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `projection` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `projection` is strongest when `projection` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `projection` lets the user do that they could not do before.
- Transfer discipline: keep the move around `projection` small and explicit so the analogy does not sprawl.
- Repository standard: if `projection` never produces a concrete move, `projection` is not yet operational.
- User question: what is one thing `projection` would make me inspect differently right now?

### projection — What To Preserve

- Preserve the pressure: `projection` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `projection` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `projection`.
- Preserve the routing value: `projection` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `projection` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `projection` stops is part of preserving `projection` well.
- Preserve the usefulness: a transferred `projection` should still help someone make a cleaner judgment.

### projection — What To Drop

- Drop the costume: `projection` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `projection` has been understood.
- Drop the universal claim: `projection` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `projection` survives only as a catchphrase, `projection` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `projection` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `projection` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `projection` and remove the surrounding noise.

### projection — Design Implication

- Design implication: `projection` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `projection` that later edits can stay honest.
- UX implication: users should feel `projection` changing the angle of analysis, not just the tone.
- Evaluation implication: test `projection` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `projection` only if they sharpen the boundary of `projection` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `projection` should refuse.
- Bottom line: `projection` belongs in the repo only if `projection` keeps improving judgment after the historical costume is removed.

### triggers and affect — Definition

- What this names: within `Complexes, projection, charged reactions, and how inner structures shape outer conflict`, `triggers and affect` points to a concrete node rather than a prestige label.
- Object boundary: treat `triggers and affect` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `triggers and affect` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `triggers and affect` before pulling it into a modern analogy.
- Skill value: `triggers and affect` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `triggers and affect` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `triggers and affect` clearly enough that a later answer can say what `triggers and affect` includes and excludes.

### triggers and affect — Historical Context

- Historical location: ask when `triggers and affect` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `triggers and affect` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `triggers and affect` says, but why `triggers and affect` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `triggers and affect` from later packaging or selective reuse.
- Reading move: map `triggers and affect` against the timeline before treating `triggers and affect` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `triggers and affect` before extracting any lesson from `triggers and affect`.
- Failure risk: without context, `triggers and affect` can look cleaner, more universal, and more innocent than it actually was.

### triggers and affect — Internal Tension

- Main tension: `triggers and affect` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `triggers and affect` makes possible and what `triggers and affect` quietly suppresses at the same time.
- Interpretive value: the tension in `triggers and affect` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `triggers and affect` double-sided rather than converting `triggers and affect` into moral reassurance.
- Research warning: if every sentence about `triggers and affect` sounds admiring or dismissive, `triggers and affect` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `triggers and affect`.
- Better question: what does `triggers and affect` solve, and what new silence does `triggers and affect` create?

### triggers and affect — Key Distinction

- Core distinction: separate `triggers and affect` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `triggers and affect`.
- Typical confusion: readers often mistake `triggers and affect` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `triggers and affect` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `triggers and affect`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `triggers and affect` by accident.
- Operational cue: define what `triggers and affect` is not before claiming to know what `triggers and affect` is.

### triggers and affect — Method

- Method question: how does `triggers and affect` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `triggers and affect` rather than inspirational phrasing.
- Research use: `triggers and affect` matters when `triggers and affect` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `triggers and affect`, not costume around `triggers and affect`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `triggers and affect`.
- Modern move: only transfer the mechanism in `triggers and affect` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `triggers and affect` without reproducing the method inside `triggers and affect`.

### triggers and affect — Operational Use

- Operational role: `triggers and affect` belongs in the skill because `triggers and affect` changes how prompts are framed and how problems are cut.
- Decision value: `triggers and affect` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `triggers and affect` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `triggers and affect` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `triggers and affect` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `triggers and affect` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `triggers and affect` make me check before I act?

### triggers and affect — Transfer To Modern Problems

- Transfer principle: bring `triggers and affect` forward only when the structural analogy around `triggers and affect` is stronger than the historical costume.
- Modern fit: `triggers and affect` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `triggers and affect` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `triggers and affect`, not the surrounding mythology around `triggers and affect`.
- Bad transfer: using `triggers and affect` to justify positions `triggers and affect` was never built to adjudicate.
- Practical test: if the analogy for `triggers and affect` cannot survive plainer language, `triggers and affect` probably does not belong in the answer.
- Design implication: every modern use of `triggers and affect` should make the analogy explicit instead of pretending `triggers and affect` is self-evident.

### triggers and affect — Failure Mode

- Failure pattern: `triggers and affect` goes bad when `triggers and affect` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `triggers and affect` for authority, mood, or aesthetic and lose the underlying discipline in `triggers and affect`.
- Research signal: when `triggers and affect` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `triggers and affect`.
- User risk: answers can start sounding impressive while `triggers and affect` becomes less falsifiable.
- Repository risk: `triggers and affect` can silently drift from operating concept into mascot.
- Correction move: pull `triggers and affect` back to evidence, mechanism, and concrete limits.
- Best warning label: `triggers and affect` should increase rigor, not lower the burden of proof.

### triggers and affect — Common Misreading

- Typical misreading: `triggers and affect` is often taken as a broad worldview when `triggers and affect` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `triggers and affect`.
- Consequence: readers then import claims into `triggers and affect` that the corpus does not actually support for `triggers and affect`.
- Skill response: keep `triggers and affect` attached to the texts, cases, and institutional situations that make `triggers and affect` legible.
- Modern correction: show what `triggers and affect` can illuminate without pretending `triggers and affect` answers everything nearby.
- Language fix: name the overreach around `triggers and affect` directly instead of just gesturing at nuance.
- Reader test: if `triggers and affect` suddenly explains every problem, `triggers and affect` is being misread.

### triggers and affect — Boundary Condition

- Boundary condition: `triggers and affect` only travels well when the receiving problem shares enough structure with the original one behind `triggers and affect`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `triggers and affect`.
- Ethical test: some uses of `triggers and affect` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `triggers and affect` into careless advice.
- Good limit: it is fine for `triggers and affect` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `triggers and affect` has no boundary because `triggers and affect` feels generically wise.
- Operational question: under what conditions should `triggers and affect` be left unused?

### triggers and affect — Better Question

- Better question: instead of asking whether `triggers and affect` is simply true, ask what problem `triggers and affect` was trying to solve.
- Reader upgrade: this turns `triggers and affect` from a label into a live diagnostic instrument.
- Research upgrade: `triggers and affect` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `triggers and affect` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `triggers and affect` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `triggers and affect` becomes useful when `triggers and affect` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `triggers and affect` is framed as a problem-solving move?

### triggers and affect — Example Move

- Example move: use `triggers and affect` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `triggers and affect` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `triggers and affect` is strongest when `triggers and affect` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `triggers and affect` lets the user do that they could not do before.
- Transfer discipline: keep the move around `triggers and affect` small and explicit so the analogy does not sprawl.
- Repository standard: if `triggers and affect` never produces a concrete move, `triggers and affect` is not yet operational.
- User question: what is one thing `triggers and affect` would make me inspect differently right now?

### triggers and affect — What To Preserve

- Preserve the pressure: `triggers and affect` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `triggers and affect` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `triggers and affect`.
- Preserve the routing value: `triggers and affect` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `triggers and affect` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `triggers and affect` stops is part of preserving `triggers and affect` well.
- Preserve the usefulness: a transferred `triggers and affect` should still help someone make a cleaner judgment.

### triggers and affect — What To Drop

- Drop the costume: `triggers and affect` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `triggers and affect` has been understood.
- Drop the universal claim: `triggers and affect` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `triggers and affect` survives only as a catchphrase, `triggers and affect` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `triggers and affect` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `triggers and affect` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `triggers and affect` and remove the surrounding noise.

### triggers and affect — Design Implication

- Design implication: `triggers and affect` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `triggers and affect` that later edits can stay honest.
- UX implication: users should feel `triggers and affect` changing the angle of analysis, not just the tone.
- Evaluation implication: test `triggers and affect` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `triggers and affect` only if they sharpen the boundary of `triggers and affect` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `triggers and affect` should refuse.
- Bottom line: `triggers and affect` belongs in the repo only if `triggers and affect` keeps improving judgment after the historical costume is removed.

### self-deception and split-off material — Definition

- What this names: within `Complexes, projection, charged reactions, and how inner structures shape outer conflict`, `self-deception and split-off material` points to a concrete node rather than a prestige label.
- Object boundary: treat `self-deception and split-off material` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `self-deception and split-off material` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `self-deception and split-off material` before pulling it into a modern analogy.
- Skill value: `self-deception and split-off material` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `self-deception and split-off material` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `self-deception and split-off material` clearly enough that a later answer can say what `self-deception and split-off material` includes and excludes.

### self-deception and split-off material — Historical Context

- Historical location: ask when `self-deception and split-off material` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `self-deception and split-off material` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `self-deception and split-off material` says, but why `self-deception and split-off material` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `self-deception and split-off material` from later packaging or selective reuse.
- Reading move: map `self-deception and split-off material` against the timeline before treating `self-deception and split-off material` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `self-deception and split-off material` before extracting any lesson from `self-deception and split-off material`.
- Failure risk: without context, `self-deception and split-off material` can look cleaner, more universal, and more innocent than it actually was.

### self-deception and split-off material — Internal Tension

- Main tension: `self-deception and split-off material` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `self-deception and split-off material` makes possible and what `self-deception and split-off material` quietly suppresses at the same time.
- Interpretive value: the tension in `self-deception and split-off material` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `self-deception and split-off material` double-sided rather than converting `self-deception and split-off material` into moral reassurance.
- Research warning: if every sentence about `self-deception and split-off material` sounds admiring or dismissive, `self-deception and split-off material` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `self-deception and split-off material`.
- Better question: what does `self-deception and split-off material` solve, and what new silence does `self-deception and split-off material` create?

### self-deception and split-off material — Key Distinction

- Core distinction: separate `self-deception and split-off material` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `self-deception and split-off material`.
- Typical confusion: readers often mistake `self-deception and split-off material` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `self-deception and split-off material` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `self-deception and split-off material`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `self-deception and split-off material` by accident.
- Operational cue: define what `self-deception and split-off material` is not before claiming to know what `self-deception and split-off material` is.

### self-deception and split-off material — Method

- Method question: how does `self-deception and split-off material` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `self-deception and split-off material` rather than inspirational phrasing.
- Research use: `self-deception and split-off material` matters when `self-deception and split-off material` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `self-deception and split-off material`, not costume around `self-deception and split-off material`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `self-deception and split-off material`.
- Modern move: only transfer the mechanism in `self-deception and split-off material` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `self-deception and split-off material` without reproducing the method inside `self-deception and split-off material`.

### self-deception and split-off material — Operational Use

- Operational role: `self-deception and split-off material` belongs in the skill because `self-deception and split-off material` changes how prompts are framed and how problems are cut.
- Decision value: `self-deception and split-off material` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `self-deception and split-off material` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `self-deception and split-off material` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `self-deception and split-off material` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `self-deception and split-off material` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `self-deception and split-off material` make me check before I act?

### self-deception and split-off material — Transfer To Modern Problems

- Transfer principle: bring `self-deception and split-off material` forward only when the structural analogy around `self-deception and split-off material` is stronger than the historical costume.
- Modern fit: `self-deception and split-off material` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `self-deception and split-off material` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `self-deception and split-off material`, not the surrounding mythology around `self-deception and split-off material`.
- Bad transfer: using `self-deception and split-off material` to justify positions `self-deception and split-off material` was never built to adjudicate.
- Practical test: if the analogy for `self-deception and split-off material` cannot survive plainer language, `self-deception and split-off material` probably does not belong in the answer.
- Design implication: every modern use of `self-deception and split-off material` should make the analogy explicit instead of pretending `self-deception and split-off material` is self-evident.

### self-deception and split-off material — Failure Mode

- Failure pattern: `self-deception and split-off material` goes bad when `self-deception and split-off material` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `self-deception and split-off material` for authority, mood, or aesthetic and lose the underlying discipline in `self-deception and split-off material`.
- Research signal: when `self-deception and split-off material` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `self-deception and split-off material`.
- User risk: answers can start sounding impressive while `self-deception and split-off material` becomes less falsifiable.
- Repository risk: `self-deception and split-off material` can silently drift from operating concept into mascot.
- Correction move: pull `self-deception and split-off material` back to evidence, mechanism, and concrete limits.
- Best warning label: `self-deception and split-off material` should increase rigor, not lower the burden of proof.

### self-deception and split-off material — Common Misreading

- Typical misreading: `self-deception and split-off material` is often taken as a broad worldview when `self-deception and split-off material` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `self-deception and split-off material`.
- Consequence: readers then import claims into `self-deception and split-off material` that the corpus does not actually support for `self-deception and split-off material`.
- Skill response: keep `self-deception and split-off material` attached to the texts, cases, and institutional situations that make `self-deception and split-off material` legible.
- Modern correction: show what `self-deception and split-off material` can illuminate without pretending `self-deception and split-off material` answers everything nearby.
- Language fix: name the overreach around `self-deception and split-off material` directly instead of just gesturing at nuance.
- Reader test: if `self-deception and split-off material` suddenly explains every problem, `self-deception and split-off material` is being misread.

### self-deception and split-off material — Boundary Condition

- Boundary condition: `self-deception and split-off material` only travels well when the receiving problem shares enough structure with the original one behind `self-deception and split-off material`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `self-deception and split-off material`.
- Ethical test: some uses of `self-deception and split-off material` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `self-deception and split-off material` into careless advice.
- Good limit: it is fine for `self-deception and split-off material` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `self-deception and split-off material` has no boundary because `self-deception and split-off material` feels generically wise.
- Operational question: under what conditions should `self-deception and split-off material` be left unused?

### self-deception and split-off material — Better Question

- Better question: instead of asking whether `self-deception and split-off material` is simply true, ask what problem `self-deception and split-off material` was trying to solve.
- Reader upgrade: this turns `self-deception and split-off material` from a label into a live diagnostic instrument.
- Research upgrade: `self-deception and split-off material` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `self-deception and split-off material` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `self-deception and split-off material` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `self-deception and split-off material` becomes useful when `self-deception and split-off material` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `self-deception and split-off material` is framed as a problem-solving move?

### self-deception and split-off material — Example Move

- Example move: use `self-deception and split-off material` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `self-deception and split-off material` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `self-deception and split-off material` is strongest when `self-deception and split-off material` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `self-deception and split-off material` lets the user do that they could not do before.
- Transfer discipline: keep the move around `self-deception and split-off material` small and explicit so the analogy does not sprawl.
- Repository standard: if `self-deception and split-off material` never produces a concrete move, `self-deception and split-off material` is not yet operational.
- User question: what is one thing `self-deception and split-off material` would make me inspect differently right now?

### self-deception and split-off material — What To Preserve

- Preserve the pressure: `self-deception and split-off material` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `self-deception and split-off material` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `self-deception and split-off material`.
- Preserve the routing value: `self-deception and split-off material` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `self-deception and split-off material` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `self-deception and split-off material` stops is part of preserving `self-deception and split-off material` well.
- Preserve the usefulness: a transferred `self-deception and split-off material` should still help someone make a cleaner judgment.

### self-deception and split-off material — What To Drop

- Drop the costume: `self-deception and split-off material` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `self-deception and split-off material` has been understood.
- Drop the universal claim: `self-deception and split-off material` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `self-deception and split-off material` survives only as a catchphrase, `self-deception and split-off material` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `self-deception and split-off material` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `self-deception and split-off material` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `self-deception and split-off material` and remove the surrounding noise.

### self-deception and split-off material — Design Implication

- Design implication: `self-deception and split-off material` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `self-deception and split-off material` that later edits can stay honest.
- UX implication: users should feel `self-deception and split-off material` changing the angle of analysis, not just the tone.
- Evaluation implication: test `self-deception and split-off material` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `self-deception and split-off material` only if they sharpen the boundary of `self-deception and split-off material` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `self-deception and split-off material` should refuse.
- Bottom line: `self-deception and split-off material` belongs in the repo only if `self-deception and split-off material` keeps improving judgment after the historical costume is removed.


## Source Files

这些调研文件回链到本仓库 `references/sources/` 里的原始素材层。

- [`archetypes-and-collective-unconscious-source-card.md`](../sources/books/archetypes-and-collective-unconscious-source-card.md)
- [`psychological-types.md`](../sources/books/psychological-types.md)
- [`two-essays-on-analytical-psychology-source-card.md`](../sources/books/two-essays-on-analytical-psychology-source-card.md)
- [`britannica-jung.md`](../sources/articles/britannica-jung.md)
- [`iep-jung.md`](../sources/articles/iep-jung.md)

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[archetypes-and-collective-unconscious-source-card.md](../sources/books/archetypes-and-collective-unconscious-source-card.md)、[psychological-types.md](../sources/books/psychological-types.md)、[two-essays-on-analytical-psychology-source-card.md](../sources/books/two-essays-on-analytical-psychology-source-card.md)
- transcripts：（无）
- articles：[britannica-jung.md](../sources/articles/britannica-jung.md)、[iep-jung.md](../sources/articles/iep-jung.md)
